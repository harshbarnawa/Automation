Never push broken code.

Every feature = one commit.

Every feature must build.

Every feature must pass tests.

Never skip formatting.

Never ignore compiler errors.

Always update roadmap.

Always update documentation.